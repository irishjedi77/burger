<li>
	{{burgerName}}

	<button class="change-burger" data-id="{{id}}" data-newBurger="{{devoured}}">
		{{#if devoured}}Eat the burger{{else}}Eat again!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Delete</button>
</li>